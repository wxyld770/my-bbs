1、创建目录 
```bash
mkdir my-bbs
```

2、初始化go modoules
```bash
go mod init my-bbs
```

3、创建项目结构
```text
my-bbs/
├── cmd/
│   └── main.go                # 程序入口
├── config
│   └── .env                   # 配置文件地址
├── internal/
│   ├── config/                # 配置加载（数据库连接、JWT密钥等）
│   ├── model/                 # 数据模型（User、Post）
│   ├── repository/            # 数据访问层（CRUD）
│   ├── service/               # 业务逻辑层（注册、发帖等）
│   ├── handler/               # HTTP 处理器（解析请求、返回响应）
│   ├── middleware/            # 中间件（JWT 认证）
│   └── router/                # 路由注册
├── pkg/                       # 可复用工具（JWT、密码加密）
├── go.mod
└── README.md
```

4、在main.go中导入需要的依赖
```go
import (
    "github.com/gin-gonic/gin"
    "gorm.io/gorm"
    "gorm.io/driver/mysql"
    "github.com/golang-jwt/jwt/v5"
    "golang.org/x/crypto/bcrypt"
)
```

5、统一拉取依赖，命令行执行`go mod tidy`，此时go.mod就会变成这样
```go
module my-bbs

go 1.26.4

require (
	github.com/gin-gonic/gin v1.12.0
	github.com/golang-jwt/jwt/v5 v5.3.1
	golang.org/x/crypto v0.54.0
	gorm.io/driver/mysql v1.6.0
	gorm.io/gorm v1.31.2
)
```

6、定义模型，在internal/model下创建user.go和post.go，嵌入 `gorm.Model` 或自行定义字段
在 `main.go` 中调用 `db.AutoMigrate(&model.User{}, &model.Post{})` 自动建表
	注意这里需要设计环境变量，统一管理配置，**使用 `.env` + `godotenv`**，这是最轻量且符合 Go 社区习惯的方式
终端执行：go get github.com/joho/godotenv
并在`config`目录下写自己`.env`文件，配置数据库连接。提交到远程仓库只是示例配置，需要在`gitignore`文件中加`.env`文件

7、读取配置初始化
在internal/config中读取.env配置，在internal/database和internal/cache中分别写初始化db和cache的方法，并在main.go中加载配置

8、编写repository层的代码
repository层是数据访问层，不做业务逻辑处理，只执行数据库的增删改查，且对于C端产品来说不建议加连表查询操作，所以这里只提供单表查询

9、编写pkg/bcrypt 和pkg/jwt，做密码加密和校验，token的生成和过期
 密码存储哈希值，验证时对比明文与哈希
 后端通过 JWT 标准中的 exp（过期时间）声明，在生成时写入“当前时间+24h”，解析时由库自动校验当前时间是否超过该值，从而确保 Token 在 24 小时内有效

10、编写service，应用程序的核心逻辑所在。需要处理密码加密、JWT生成、权限验证等

11、编写认证中间件，设计增删改的接口都要验证token，部分查询接口需要验证token，登陆、注册接口不需要验证token

12、编写handler，handler就是对外提供接口的类，只做简单的流程编排，核心逻辑下沉到service。对于简单的crud就是调service方法，看着是多了一层。可以类比Java的app层？

13、路由注册，如果把注册都写在router.go，把依赖注入都写在main.go，那么随着handler、service的增加，必然会造成router、main的臃肿
	所以，如何让服务稳定，新增功能，增加API让代码改动少就至关重要，核心思想：* 让“变化”封装在模块内部，让“组装”保持稳定 *

14、主函数组装
	main.go：初始化配置、数据库连接、自动迁移，创建 router，启动 gin 服务。